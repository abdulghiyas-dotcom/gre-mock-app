{
  "name": "@gre-mock/content-gen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LLM question-generation pipeline for the GRE mock platform (generate -> solve-blind -> critique -> validate).",
  "scripts": {
    "generate": "tsx src/run.ts",
    "validate": "tsx src/validate.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
